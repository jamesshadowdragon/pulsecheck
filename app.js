const API="https://pulsecheck.page.gd";const $=s=>document.querySelector(s);let monitors=[];const tokens=()=>{try{return JSON.parse(localStorage.getItem("pulsecheck_tokens")||"{}")}catch{return{}}};const token=id=>tokens()[id]||"";function saveToken(id,t){const x=tokens();x[id]=t;localStorage.setItem("pulsecheck_tokens",JSON.stringify(x))}function delToken(id){const x=tokens();delete x[id];localStorage.setItem("pulsecheck_tokens",JSON.stringify(x))}async function api(path,opt={}){const r=await fetch(API+path,{...opt,headers:{"Content-Type":"application/json",...(opt.headers||{})}});let d={};try{d=await r.json()}catch{}if(!r.ok)throw new Error(d.error||`Request failed (${r.status})`);return d}function host(u){try{return new URL(u).hostname.replace(/^www\./,"")}catch{return u}}function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}function toast(t){const e=$("#toast");e.textContent=t;e.classList.add("show");clearTimeout(window.__tt);window.__tt=setTimeout(()=>e.classList.remove("show"),2600)}function stats(){$("#total").textContent=monitors.length;$("#up").textContent=monitors.filter(x=>x.status==="up").length;$("#down").textContent=monitors.filter(x=>x.status==="down").length;const a=monitors.map(x=>+x.response_time||0).filter(Boolean);$("#avg").textContent=a.length?Math.round(a.reduce((x,y)=>x+y,0)/a.length)+" ms":"—"}function render(){stats();const l=$("#list");if(!monitors.length){l.innerHTML='<div class="empty"><b>No monitors yet</b>Add a URL above to create your first monitor.</div>';return}l.innerHTML=monitors.map(m=>`<article class="card" data-id="${m.id}"><div><div class="name">${esc(host(m.url))}</div><div class="url">${esc(m.url)}</div></div><div class="metric"><small>Status</small><strong class="status ${esc(m.status)}">${m.status==="up"?"Online":m.status==="down"?"Offline":"Pending"}</strong></div><div class="metric"><small>Response</small><strong>${m.response_time?Math.round(m.response_time)+" ms":"—"}</strong></div><div class="actions"><button class="action" data-a="history">History</button><button class="action" data-a="check">${token(m.id)?"Check now":"Add token"}</button><button class="action danger" data-a="delete">Delete</button></div><div class="history">Loading…</div></article>`).join("")}async function load(note=false){try{const d=await api("/api/monitors.php");monitors=d.monitors||[];render();$("#apiState").textContent="API online";if(note)toast("Refreshed") }catch(e){$("#apiState").textContent="API unavailable";$("#list").innerHTML=`<div class="empty"><b>Could not reach API</b>${esc(e.message)}<br><br>If this is CORS, add this site's domain to allowedOrigins in config.php.</div>`}}$("#addForm").addEventListener("submit",async e=>{e.preventDefault();let u=$("#urlInput").value.trim();if(!/^https?:\/\//i.test(u))u="https://"+u;const b=$("#addBtn");b.disabled=true;$("#msg").textContent="Creating monitor…";try{const d=await api("/api/monitors.php",{method:"POST",body:JSON.stringify({url:u})});if(d.monitor?.token)saveToken(d.monitor.id,d.monitor.token);$("#urlInput").value="";$("#msg").textContent="Monitor created.";toast("Monitor created — token saved on this browser");await load()}catch(err){$("#msg").textContent=err.message;toast(err.message)}finally{b.disabled=false}});$("#refresh").onclick=()=>load(true);$("#list").addEventListener("click",async e=>{const b=e.target.closest("button[data-a]");if(!b)return;const c=b.closest(".card"),id=+c.dataset.id,a=b.dataset.a;if(a==="history"){c.classList.toggle("open");if(!c.classList.contains("open"))return;const h=c.querySelector(".history");h.innerHTML="Loading…";try{const d=await api(`/api/history.php?id=${id}&limit=12`);h.innerHTML=(d.checks||[]).length?(d.checks||[]).map(x=>`<div class="historyItem"><b>${x.success?"UP":"DOWN"}</b><br>${esc(x.status_code)} · ${esc(x.response_time)}ms<br>${esc(new Date(x.checked_at).toLocaleString())}</div>`).join(""):"No checks yet."}catch(err){h.textContent=err.message}return}let t=token(id);if(!t){t=prompt("Paste the secret token for monitor #"+id);if(!t)return;saveToken(id,t)}if(a==="check"){b.disabled=true;b.textContent="Checking…";try{await api(`/api/check.php?id=${id}`,{method:"POST",headers:{"X-PulseCheck-Token":t}});toast("Check complete");await load()}catch(err){if(/token/i.test(err.message))delToken(id);toast(err.message)}finally{b.disabled=false}}if(a==="delete"){if(!confirm("Delete this monitor and its history?"))return;b.disabled=true;try{await api(`/api/monitors.php?id=${id}`,{method:"DELETE",headers:{"X-PulseCheck-Token":t}});delToken(id);toast("Monitor deleted");await load()}catch(err){if(/token/i.test(err.message))delToken(id);toast(err.message)}finally{b.disabled=false}}});load();setInterval(load,30000);