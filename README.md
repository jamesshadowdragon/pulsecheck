# PulseCheck Dashboard

API: https://pulsecheck.page.gd/

Deploy `index.html`, `styles.css`, and `app.js` to Vercel or any static host.

IMPORTANT: Add the exact frontend origin to `$allowedOrigins` in your API `config.php`, otherwise browser CORS will block requests.

Monitor tokens are stored only in browser localStorage after monitor creation.
